import React from "react";
import "./orderButton.scss";

const OrderButton = () => {
  return (
    <button id="orderButton" className="orderButton">
      Записаться
    </button>
  );
};

export default OrderButton;
